+*#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> vec = {1, 3, 5, 7, 9};

    // Check if the element 5 is present in the vector
    if (std::binary_search(vec.begin(), vec.end(), 5)) {
        std::cout << "Element found in vector" << std::endl;
    } else {
        std::cout << "Element not found in vector" << std::endl;
return 0;
}